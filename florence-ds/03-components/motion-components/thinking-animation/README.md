# Thinking Animation

A compact three-dot status indicator for short agent or system processing
states. The animation uses semantic motion tokens and becomes static when the
user prefers reduced motion.

```jsx
import { ThinkingAnimation } from './ThinkingAnimation.jsx'

<ThinkingAnimation label="Thinking" size="md" />
```
