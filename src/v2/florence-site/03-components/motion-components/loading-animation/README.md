# Loading Animation

A compact square loading indicator with a 3×3 dot grid. The dots smoothly
crossfade between theme-aware black and white using semantic tokens, and become
static when the user prefers reduced motion.

```jsx
import { LoadingAnimation } from './LoadingAnimation.jsx'

<LoadingAnimation label="Loading" size="md" />
```
