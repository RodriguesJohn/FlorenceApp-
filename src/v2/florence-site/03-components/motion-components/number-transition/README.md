# Number Transition

A numeric text transition inspired by spring-driven content transitions. Only
the changed characters roll, using a 400ms semantic motion duration and smooth
on-screen easing. Increasing values move upward and decreasing values move
downward.

```jsx
import { NumberTransition } from './NumberTransition.jsx'

<NumberTransition value={128} size="md" />
```
